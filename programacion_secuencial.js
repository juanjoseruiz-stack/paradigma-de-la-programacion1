let nombre = "juan";
let apellido = "ruiz"; 
let edad = 20;
let numero = 30;
function sumar(a, b) {
    return a + b;
}

function restar(a, b) {
    let operacion = a - b;
    return operacion
}

console.log("el resultado de la suma es: " + sumar(numero, edad));

let resultadoResta = restar(5, 7);

console.log("nmbre comleto: " + nombre, "" + apellido);